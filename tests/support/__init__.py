"""Support layer for the capability-matching test suite.

Isolates integration with ``src`` (loader/runner) and the evaluation logic
(diagnostics) so the test module stays a thin orchestrator. See
``docs/plano-refatoracao-testes.md`` (Fase 1).
"""
